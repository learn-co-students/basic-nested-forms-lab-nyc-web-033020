<%= form_for @recipe do |r| %>
<%= r.label "Recipe title:" %>
<%= r.text_area :title %><br>

<%= r.fields_for :ingredients do |i| %> 
  <%= i.label :name %> 
  <%= i.text_field :name %> <br>

  <%= i.label :quantity %>
  <%= i.text_field :quantity %> <br> 

  <%end%>
  
<%=r.submit %> 
<%end%> 